menu:
  - title: "Run TUI"
    shell: "cargo run"
  - title: "Run GUI"
    shell: "cargo run -- --gui"
  - title: "Build release"
    shell: "cargo build --release"
  - title: "Release (minor bump, push to main)"
    shell: "scripts/release.sh"
  - title: "Release (patch bump, push to main)"
    shell: "scripts/release.sh patch"
